# Deprecated implementation prompt

Do not use the historical priority list that previously lived here; most of it has been completed and the remainder has changed.

For current implementation work:
1. follow `AGENTS.md`
2. inspect the relevant current code/state
3. read only the relevant section of `MoLumen_OS/BACKLOG.md`
4. use the appropriate project skill/rule
5. verify any old finding still exists
6. run proportional validation

Current priorities belong in `BACKLOG.md`, not in copied prompts that become stale.
